import { RiDeleteBin5Line } from 'react-icons/ri';

type DeleteIconProps = {
  size?: number;
};

export default function DeleteIcon({ size }: DeleteIconProps) {
  return <RiDeleteBin5Line size={size} />;
}
